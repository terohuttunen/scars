name = "e310x-qemu"
target = "riscv32imac-unknown-none-elf"
package = "scars"
features = ["khal-e310x"]
# RAM-constrained: drop the opt-out features. `--no-default-features`
# also drops `relative-delay`, so re-list it for the kernel build.
default-features = false
kernel_features = ["bench-large", "relative-delay"]
test_runner = "qemu"
examples_dirs = ["examples/e310x"]

[linker]
script = "khal/scars-khal-e310x/default.lds"

[env]
MTIME_FREQ_HZ = "10_000_000"

[runner]
program = "qemu-system-riscv32"
args = [
    "-machine", "sifive_e",
    "-cpu", "sifive-e31",
    "-semihosting-config", "enable=on,chardev=stdio0",
    "-bios", "none",
    "-chardev", "stdio,id=stdio0",
    "-display", "none",
    "-gdb", "tcp::1234",
    "-kernel",
]
